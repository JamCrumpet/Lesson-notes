# matplotlib lets you colour points individually for a scatter plot
# the default colour is blue does with black outline
# when plotting multiple points black outlines can blend together

# to remove the outlines around points pass the arguments edgecolour="none" when you call scatter

# plt.scatter(x_values, y_values, edgecolour="none", s=40)

