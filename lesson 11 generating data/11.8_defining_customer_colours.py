# to changes the colour of points pass c to scatter() with the name of the colour you want to use


# plt.scatter(x_values, y_values, c="red", edgecolour="none", s=40)

# you can also defines custom colours using the RGB colour model

# plt.scatter(x_values, y_values, c=(0.8,0.2,0.9), edgecolour="none", s=40)

# colours closer to 0 produces dark colours and values colour to 1 produces lighter colours
